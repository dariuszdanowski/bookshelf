{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "./node_modules/.bin/astro dev",
      "name": "Development server (jak jest aktywne w .dev.vars)",
      "request": "launch",
      "type": "node-terminal"
    },
    {
      "command": "npm run dev:local",
      "name": "Dev server (local Supabase)",
      "request": "launch",
      "type": "node-terminal",
      "preLaunchTask": "Pre-dev: env:local + supabase start"
    }
  ]
}
